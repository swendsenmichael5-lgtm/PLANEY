{
  "name": "planey",
  "version": "0.1.0",
  "description": "First-person social deduction on a plane: find the knife in the sandal before the Evil reaches the cockpit.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "bundle:trystero": "echo 'export * from \"trystero/nostr\";' > entry.mjs && esbuild entry.mjs --bundle --format=esm --minify --outfile=public/vendor/trystero-nostr.js && rm entry.mjs"
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:37565/git/swendsenmichael5-lgtm/PLANEY"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "trystero": "^0.25.2",
    "esbuild": "^0.25.0"
  }
}
